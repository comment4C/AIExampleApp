import React from 'react'
import {
    View,
    Text,
    StyleSheet,
    Button
  } from 'react-native'

export default function ObjectDetectScreen( {navigation} ){
    return (
        <Text>This is ObjectDetectScreen</Text>
    );
}